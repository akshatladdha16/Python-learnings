# if u want to use any file functions in this file then u can import that in this
import functions
print(functions.say_hi("akshat",20))
#this is how spftware dev work they don't write whole code in one file they import functions from other files to keep their code organised

# pip is package manager to install update and modify python packages
# use pip install in command prompt (pip--version)

